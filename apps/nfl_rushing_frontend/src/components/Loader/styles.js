import styled from "styled-components";
import RSLoader from "react-loader-spinner";

export const StyledLoader = styled(RSLoader)`
  display: inline;
  margin-left: 30px;

  svg {
    height: 15px;
  }
`;
